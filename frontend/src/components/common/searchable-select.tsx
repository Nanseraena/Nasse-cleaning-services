"use client";
import { KeyboardEvent,useEffect,useId,useMemo,useRef,useState } from "react";

export type SearchableSelectOption={value:string;label:string;disabled?:boolean};
type SearchableSelectProps={options:SearchableSelectOption[];value:string;onChange:(value:string)=>void;label?:string;placeholder?:string;searchPlaceholder?:string;emptyMessage?:string;id?:string;required?:boolean;disabled?:boolean;className?:string;triggerClassName?:string;dropdownInFlow?:boolean;clearable?:boolean};

export function SearchableSelect({options,value,onChange,label,placeholder="Select an option",searchPlaceholder="Search options…",emptyMessage="No options found.",id,required=false,disabled=false,className="",triggerClassName="",dropdownInFlow=false,clearable=false}:SearchableSelectProps){
 const generatedId=useId(),selectId=id??generatedId,containerRef=useRef<HTMLDivElement>(null),searchRef=useRef<HTMLInputElement>(null),[open,setOpen]=useState(false),[search,setSearch]=useState(""),[activeIndex,setActiveIndex]=useState(0);
 const selected=options.find(option=>option.value===value),filtered=useMemo(()=>options.filter(option=>option.label.toLowerCase().includes(search.trim().toLowerCase())),[options,search]);
 useEffect(()=>{if(!open)return;searchRef.current?.focus();setActiveIndex(Math.max(0,filtered.findIndex(option=>!option.disabled)));const outside=(event:MouseEvent)=>{if(!containerRef.current?.contains(event.target as Node))setOpen(false)};const escape=(event:globalThis.KeyboardEvent)=>{if(event.key==="Escape")setOpen(false)};document.addEventListener("mousedown",outside);document.addEventListener("keydown",escape);return()=>{document.removeEventListener("mousedown",outside);document.removeEventListener("keydown",escape)}},[open]);
 const choose=(option:SearchableSelectOption)=>{if(option.disabled)return;onChange(option.value);setSearch("");setOpen(false)};
 const keyDown=(event:KeyboardEvent<HTMLInputElement>)=>{if(event.key==="ArrowDown"){event.preventDefault();setActiveIndex(index=>Math.min(filtered.length-1,index+1))}if(event.key==="ArrowUp"){event.preventDefault();setActiveIndex(index=>Math.max(0,index-1))}if(event.key==="Enter"&&filtered[activeIndex]){event.preventDefault();choose(filtered[activeIndex])}};
 return <div ref={containerRef} className={`relative ${className}`}><input className="sr-only" tabIndex={-1} aria-hidden="true" value={value} onChange={()=>{}} required={required}/>{label&&<label id={`${selectId}-label`} className="mb-2 block text-sm font-semibold text-brand-navy">{label}{required&&<span className="text-red-500"> *</span>}</label>}<div className="relative"><button id={selectId} type="button" disabled={disabled} onClick={()=>setOpen(current=>!current)} aria-haspopup="listbox" aria-expanded={open} aria-labelledby={label?`${selectId}-label ${selectId}`:undefined} className={`flex min-h-12 w-full items-center justify-between gap-3 rounded-xl border border-slate-300 bg-white px-4 py-3 text-left outline-none transition hover:border-brand-green focus:border-brand-green focus:ring-2 focus:ring-green-100 disabled:bg-slate-100 disabled:opacity-60 ${triggerClassName}`}><span className={selected?"text-slate-900":"text-slate-400"}>{selected?.label??placeholder}</span><span className="flex items-center gap-2">{clearable&&value&&<span role="button" aria-label="Clear selection" onClick={event=>{event.stopPropagation();onChange("")}} className="grid h-5 w-5 place-items-center rounded-full text-slate-400 hover:bg-slate-100">×</span>}<svg aria-hidden="true" viewBox="0 0 20 20" fill="none" stroke="currentColor" strokeWidth="2" className={`h-4 w-4 text-slate-500 transition ${open?"rotate-180":""}`}><path strokeLinecap="round" strokeLinejoin="round" d="m5 7.5 5 5 5-5"/></svg></span></button>{open&&<div className={`${dropdownInFlow?"relative":"absolute"} left-0 z-50 mt-2 w-full min-w-56 rounded-2xl border border-slate-200 bg-white p-2 shadow-xl`}><div className="relative"><svg aria-hidden="true" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" className="absolute left-3 top-2.5 h-4 w-4 text-slate-400"><circle cx="11" cy="11" r="7"/><path d="m16 16 4 4"/></svg><input ref={searchRef} value={search} onChange={event=>{setSearch(event.target.value);setActiveIndex(0)}} onKeyDown={keyDown} placeholder={searchPlaceholder} className="w-full rounded-xl border bg-slate-50 py-2 pl-9 pr-3 text-sm outline-none focus:border-brand-green"/></div><div role="listbox" aria-labelledby={label?`${selectId}-label`:undefined} className="mt-2 max-h-60 overflow-y-auto">{filtered.map((option,index)=><button type="button" role="option" aria-selected={option.value===value} disabled={option.disabled} key={option.value} onMouseEnter={()=>setActiveIndex(index)} onClick={()=>choose(option)} className={`flex w-full items-center justify-between rounded-xl px-3 py-2.5 text-left text-sm disabled:opacity-40 ${index===activeIndex?"bg-slate-100":"hover:bg-slate-50"}`}><span>{option.label}</span>{option.value===value&&<svg aria-hidden="true" viewBox="0 0 20 20" fill="none" stroke="currentColor" strokeWidth="2.5" className="h-4 w-4 text-brand-green"><path strokeLinecap="round" strokeLinejoin="round" d="m4 10 4 4 8-8"/></svg>}</button>)}{!filtered.length&&<p className="px-3 py-6 text-center text-sm text-slate-500">{emptyMessage}</p>}</div></div>}</div></div>
}
